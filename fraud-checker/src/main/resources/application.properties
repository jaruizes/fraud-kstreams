quarkus.http.port=8090
kafka.bootstrap.servers=localhost:29092
quarkus.kafka-streams.bootstrap-servers=localhost:29092
quarkus.kafka-streams.application-id=fraud-checker


mp.messaging.outgoing.card-movements.connector=smallrye-kafka
mp.messaging.outgoing.card-movements.topic=movements
mp.messaging.outgoing.card-movements.auto.register.schemas=true
mp.messaging.connector.smallrye-kafka.schema.registry.url=http://localhost:8081


schema-registry=http://localhost:8081

quarkus.kafka-streams.topics=movements
topics.input.movements=movements
topics.output.fraud-cases=fraud-cases


kafka-streams.cache.max.bytes.buffering=10240
kafka-streams.commit.interval.ms=1000
kafka-streams.metadata.max.age.ms=500
kafka-streams.auto.offset.reset=earliest
kafka-streams.metrics.recording.level=DEBUG
kafka-streams.consumer.heartbeat.interval.ms=200